import { TSubscriptionStatus } from './subscription.interface';

export const SubscriptionStatus: TSubscriptionStatus[] = [
  'active',
  'expired',
  'canceled',
  'pending',
];

export const DurationType = ['monthly', 'yearly'];
